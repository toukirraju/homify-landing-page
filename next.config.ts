import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["cdn.tuk.dev",
      "images.unsplash.com",
      "plus.unsplash.com",
      "cdn.dribbble.com",
      "cdn.pixabay.com",
      "i.pravatar.cc",
      "via.placeholder.com",
    ],

  },
};

export default nextConfig;
